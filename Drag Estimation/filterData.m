function [vel, accel] = filterData(FCData,GPSData)
    % Combines data from flight computers and gps and outputs velocity and
    % axial acceleration magnitudes

    vel = FCData.Velocity;
    accel = ;
end

